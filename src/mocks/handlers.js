// import { rest } from "msw";

// Global Storybook Mocks
export const handlers = [
  // Capture a GET /api/todos request,
  // rest.get("/api/todos", (req, res, ctx) => {
  //   return res(
  //     ctx.json([
  //       { label: "Eat", done: true },
  //       { label: "Sleep", done: true },
  //       { label: "Code", done: false }
  //     ])
  //   );
  // })
];
