import React from "react";

/**
 * TODO - annotate this component
 * Simple presentational component, but keeps things contained + reusable
 * @param {*} props
 */
export function ItemListError() {
  return (
    <div>
      <p>ERROR!</p>
    </div>
  );
}
